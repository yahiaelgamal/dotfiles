for matplotlibrc it should be linked inside .matplotlib


For instance:
`ln -s ~/dotfiles/matplotlibrc .matplotlib`

There is also a .config director. just
`cd && ln -s ~/dotfiles/.config .config`

there is a .config folder that is very important for karabinar
