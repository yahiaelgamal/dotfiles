library(stats) # to avoid it masking anything
options(max.print=1000) # maximum character to print
#source('~/dotfiles/common.R')

#install.packages("ggvis", dependencies=TRUE, repos='http://cran.rstudio.com/')

#library(devtools)
#install_github("ggobi/ggally")
#
#

library(readr)
library(dplyr)
library(ggplot2)
library(tidyr)
library(magrittr)
